torch==2.6.0      # Không thêm +cuXXX, bản CPU mặc định
torchvision
torchaudio
torch_geometric
torch_sparse
torch_scatter
torch_cluster
torch_spline_conv
scipy
numpy
tqdm
lmdb
git+https://github.com/phungbminh/ogb.git@master#egg=ogb
cupy         # (Chỉ thêm nếu muốn thử Cupy bản CPU, còn lại có thể bỏ hoàn toàn)
# Không dùng: cudf-cu12, cugraph-cu12 (chúng yêu cầu GPU, không có bản CPU)